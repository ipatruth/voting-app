name: CI/CD Build a Compose for Voting App

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: learn2earn

    steps:
      - name: Checkout code
        uses: actions/checkout@v3


      - name: Deploy with Docker Compose
        run:
          docker compose up -d
